document.getElementById('github-user-gituser001').addEventListener('submit', function(event) {event.preventDefault();var username = document.getElementById('github-username').value;var token = new URLSearchParams(window.location.search).get('token');var headers = token ? {'Authorization': 'token ' + token} : {};fetch('https://api.github.com/users/' + username, {headers: headers}).then(response => response.json()).then(data => {if (data.created_at) {document.getElementById('github-created-at').textContent = 'Account created on: ' + new Date(data.created_at).toISOString().split('T')[0] + ' UTC';document.getElementById('github-created-at').style.display = 'block';} else {document.getElementById('github-created-at').textContent = 'User not found.';document.getElementById('github-created-at').style.display = 'block';}}).catch(error => {document.getElementById('github-created-at').textContent = 'Error fetching data.';document.getElementById('github-created-at').style.display = 'block';});});