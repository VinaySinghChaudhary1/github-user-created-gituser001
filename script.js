async function fetchGitHubUser() {const username = document.getElementById('github-username').value;const token = new URLSearchParams(window.location.search).get('token');let headers = {};if (token) {headers['Authorization'] = `token ${token}`;}try {const response = await fetch(`https://api.github.com/users/${username}`, {headers});if (!response.ok) {throw new Error('User not found');}const data = await response.json();document.getElementById('github-created-at').textContent = `Account created on: ${new Date(data.created_at).toISOString().split('T')[0]} UTC`;} catch (error) {document.getElementById('github-created-at').textContent = 'Error fetching user data';}}